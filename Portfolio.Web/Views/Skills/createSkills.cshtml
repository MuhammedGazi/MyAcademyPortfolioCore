@model Skill
@{
    ViewData["Title"] = "createSkills";
    Layout = "~/Views/AdminLayout/Layout.cshtml";
}

<div class="container mt-4">
    <hr />

    <div class="row">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <form asp-action="createSkills">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="form-group mb-3">
                            <label asp-for="Title" class="control-label">Yetenek Adı</label>
                            <input asp-for="Title" class="form-control" placeholder="Örn: C# & .NET Core" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Percentage" class="control-label">Yüzdelik Değer</label>
                            <input asp-for="Percentage" type="number" min="0" max="100" class="form-control" placeholder="Örn: 90" />
                            <span asp-validation-for="Percentage" class="text-danger"></span>
                        </div>

                        <div class="form-group mt-4">
                            <button type="submit" class="btn btn-success">
                                <i class="fas fa-save"></i> Kaydet
                            </button>
                            <a asp-action="Skills" class="btn btn-secondary">
                                <i class="fas fa-arrow-left"></i> Listeye Geri Dön
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>


